export default function StickyHeaderHeading(headingContent) {
    return <p style={{
        fontWeight: "600",
        fontSize: "1.8em",
        lineHeight: "1",
        margin: "0",
        textAlign: "left"
    }}>
        {headingContent}
    </p>
}